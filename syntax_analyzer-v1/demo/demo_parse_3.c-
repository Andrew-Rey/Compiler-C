void main() {
    int count = 10;
    while (count > 0) {
        count = count - 1;
        /* if count can be divided by 2, then divide it! */
        if (count / 2 != 0) {
            continue;
        }
        count = count / 2;
    }
    return;
}